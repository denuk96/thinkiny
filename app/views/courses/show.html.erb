<div class="container">
  <h1><%= @course.name %></h1>
  <%= link_to "Edit", edit_course_path(@course), class: "btn btn-outline-dark"  %> |
  <%= link_to "Destroy", course_path(@course), class: "btn btn-outline-dark" ,  method: :delete, data: { confirm: 'Are you sure?' }%>
  <%= link_to 'join', join_path(id: @course ), class: "btn btn-outline-dark" %>
  <hr>
  <p><%= @course.description %></p>
  <%= link_to "Show lessons", course_lessons_path(@course) %> |
  <%= link_to "Add lessons", new_course_lesson_path(@course) %>
  <p><b>Joined users:</b></p>
  <% @course.users.each do |user| %>
    <%= user.email %>
  <% end %>
  <br>

  <%= link_to "Back", courses_path, class: "btn btn-outline-dark" %>
</div>
