<% set_page_title("Categories | Thinkiny") %>
<div class="container">
  <ul><h1><%= t("category.categories") %>:</h1></ul>
  <% @categories.each do |category| %>
  <li><%= category.title %></li>
  <% end %>
</div>
