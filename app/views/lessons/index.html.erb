<div class="container">
  <h1>all lessons</h1>
  <% @lessons.each do |lesson| %>
    <h3><%= link_to lesson.theme, course_lesson_path(@course, lesson) %></h3>
    <br>
    <%= link_to 'start', start_lesson_path(course_id: lesson.course.id, lesson_id: lesson.id) %>
    <br>
    <hr>
  <% end %>
</div>
